/**
 * The proposal notice is intentionally hidden for the client-facing preview.
 * Pitch-mode safeguards remain in the content and project labels until the
 * approved RABS assets and facts are ready for launch.
 */
export function PitchBanner() {
  return null;
}
